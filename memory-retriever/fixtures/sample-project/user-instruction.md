Analyze code quality metrics for the current sprint.

Requirements:

- Focus on test coverage and static analysis results.
- Use the shared provider-backed workflow first.
- Include relevant project context where it materially improves the report.
